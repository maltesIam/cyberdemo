/**
 * MedicumPage - Wrapper to render the Medicum Demo App
 * as a standalone page within the SoulBot platform.
 */
import { MedicumApp } from "../components/medicum/App";

export function MedicumPage() {
  return <MedicumApp />;
}
